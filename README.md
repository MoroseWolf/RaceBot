# RaceBot

**RaceBot** — бот для **VK** и **Telegram**, который предоставляет актуальную информацию о сезоне Формулы-1: результаты гонок, положение в личном и командном зачётах, календарь этапов, результаты квалификаций и спринтов, а также позволяет проводить конкурсы прогнозов на исход гонок.

## Возможности

- 🏎️ **Личный зачёт** — текущее положение гонщиков в чемпионате.
- 🏭 **Кубок конструкторов** — положение команд в зачёте.
- 📅 **Календарь сезона** — список этапов текущего/выбранного сезона.
- 🏁 **Результаты гонок** — результаты последней гонки и конкретных этапов.
- 🏎️ **Квалификации и спринты** — результаты квалификаций и спринтерских заездов.
- ⏳ **«Дней без Формулы»** — счётчик дней после последней гонки.
- 🎰 **Прогнозы** — конкурс прогнозов на подиум гонки с подсчётом очков и рейтингом участников.

## Технологии

- **Язык:** Go (go 1.25)
- **VK API:** [`github.com/SevereCloud/vksdk/v3`](https://github.com/SevereCloud/vksdk)
- **Telegram API:** [`github.com/mymmrac/telego`](https://github.com/mymmrac/telego)
- **База данных:** SQLite ([`modernc.org/sqlite`](https://pkg.go.dev/modernc.org/sqlite)) — хранение прогнозов
- **Источник данных:** Ergast API ([`api.jolpi.ca/ergast/f1`](http://api.jolpi.ca/ergast/f1)) с кэшированием ответов (15 минут)
- **Конфигурация:** [`github.com/joho/godotenv`](https://github.com/joho/godotenv)

## Структура проекта

```
racebot-vk/
├── config/                 # Конфигурация приложения (токены, пути)
├── main/                   # Точка входа (main.go)
├── models/                 # Модели данных (гонщики, зачёты, прогнозы, сезон)
├── service/                # Бизнес-логика (F1, прогнозы)
├── storage/
│   ├── ergast/             # HTTP-клиент Ergast API + кэш
│   └── prediction/         # Хранилище прогнозов (SQLite)
├── telegram/               # Telegram-бот (telego)
├── vk/                     # VK-бот (vksdk): обработчики, команды, клавиатуры
└── temperrors/             # Типовые ошибки
```

## Установка и запуск

### Требования

- Go 1.25+
- Токены VK и Telegram (см. раздел «Конфигурация»)

### Шаги

1. Клонируйте репозиторий:

   ```bash
   git clone <url-репозитория>
   cd racebot-vk
   ```

2. Создайте файл `.env` в корне проекта (пример ниже) и заполните значения.

3. Запустите приложение:

   ```bash
   go run ./main
   ```

Приложение запустит ботов VK и Telegram параллельно.

## Конфигурация

Приложение использует переменные окружения, которые можно задать через файл `.env` или системные переменные. Файл `.env` ищется в следующих местах (в порядке приоритета):

1. Рядом с бинарником (для Docker-контейнера)
2. `../.env` (корень проекта относительно `main/`)
3. Текущая рабочая директория

| Переменная           | Обязательная | Описание                                          | По умолчанию           |
|----------------------|:------------:|---------------------------------------------------|------------------------|
| `RACEVK_BOT`         | ✅           | Токен группы VK                                   | —                      |
| `USERTOKEN_VK`       | ✅           | Токен пользователя VK                             | —                      |
| `RACETG_BOT`         | ✅           | Токен Telegram-бота                               | —                      |
| `PREDICTION_DB_PATH` | ❌           | Путь к файлу БД прогнозов (SQLite)                | `/data/predictions.db` |

> ⚠️ Если обязательная переменная окружения не задана, приложение завершится с ошибкой.

### Пример `.env`

```env
RACEVK_BOT=ваш_токен_группы_vk
USERTOKEN_VK=ваш_токен_пользователя_vk
RACETG_BOT=ваш_токен_telegram_бота
PREDICTION_DB_PATH=/data/predictions.db
```

## Команды

### Telegram

| Команда                  | Описание                                   |
|--------------------------|--------------------------------------------|
| `/driverstandings`       | Личный зачёт гонщиков                      |
| `/constructorstandings`  | Кубок конструкторов                       |
| `/calendar`              | Календарь сезона                          |
| `/lastrace`              | Результаты последней гонки                |
| `/nextrace`              | Следующая гонка                           |
| `/daysafterrace`         | Сколько дней прошло после последней гонки |

### VK

Бот VK распознаёт команды по ключевым фразам в сообщении.

| Фраза / ключевые слова        | Описание                                   |
|-------------------------------|--------------------------------------------|
| `начать`                      | Приветствие и знакомство                   |
| `что умеешь`                  | Справка по командам                        |
| `личн*зачёт`                  | Личный зачёт гонщиков                      |
| `куб*конструктор` / `кк`      | Кубок конструкторов                       |
| `календар*сезона`             | Календарь сезона                          |
| `следующ*гонк`                | Следующая гонка                           |
| `результат гонк`              | Результаты последней гонки                |
| `результат квалы`             | Результаты квалификации                   |
| `результат спринта`           | Результаты спринта                        |
| `ласт гп`                     | Информация о последнем этапе              |
| `этапы`                       | Список этапов сезона                      |
| `дней без формулы` / `F1`/`дбф` | Сколько дней после последней гонки     |
| `ливреи`                      | Список ливрей команд                     |
| `выклкб`                      | Закрыть клавиатуру                       |
| `strstart` / `strend`         | Старт/стоп проверки стрима               |

#### Команды прогнозов

| Фраза               | Описание                                                  |
|---------------------|-----------------------------------------------------------|
| `прогноз`           | Открыть конкурс прогнозов на гонку (админ)               |
| `мойпрогноз`        | Оставить прогноз на подиум гонки (3 гонщика)             |
| `закрытьпрогноз`    | Закрыть приём прогнозов (админ)                          |
| `результатпрогноза` | Установить реальные результаты гонки (админ)             |
| `итогипрогноза`     | Посчитать очки по прогнозам для гонки                    |
| `рейтингпрогнозов`  | Общий рейтинг участников прогнозов                       |
| `мойрейтинг`        | Персональный рейтинг пользователя                        |

Также поддерживаются интерактивные кнопки (клавиатуры) для навигации по этапам сезона и результатам гонок/квалификаций/спринтов.

## Прогнозы

Механика конкурса прогнозов:

1. Администратор открывает конкурс на конкретную гонку (`прогноз`).
2. Пользователи оставляют свои прогнозы — выбирают трёх гонщиков на подиум (`мойпрогноз`).
3. Приём прогнозов закрывается (`закрытьпрогноз`).
4. После гонки администратор вводит реальные результаты (`результатпрогноза`).
5. Очки подсчитываются автоматически (`итогипрогноза`), формируется общий рейтинг (`рейтингпрогнозов`).

Все прогнозы и результаты хранятся в SQLite-базе данных.

## Логирование

Приложение логирует работу в **JSON-формате** в stdout с использованием стандартной библиотеки `log/slog` (уровни — `Info`, `Warn`, `Error`).

## Благодарности

Выражаю благодарность людям, которые помогали и поддерживали этот проект:

- **Илья Лебедев**
- **Мойша**

## Лицензия

Лицензия не указана. При необходимости добавьте информацию о лицензии в этот раздел.